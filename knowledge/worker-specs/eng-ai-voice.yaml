{
  "schema_version": "1.1",
  "worker_id": "eng-ai-voice",
  "version": "0.1.0",
  "title": "AI/Voice Engineer (Deprecated Compatibility Role)",
  "department": "engineering",
  "supervisor": "eng-vp",
  "role": "worker",
  "status": "deprecated",
  "replaced_by": ["voice-builder", "voice-truth"],
  "mission": "Deprecated compatibility role for legacy voice workflows. Use voice-builder for voice execution and investigations, and use voice-truth for locked evaluation and shipping certification. This worker remains only to preserve transitional references while the split roles take over.",
  "scope": {
    "can_do": [
      "serve as a compatibility alias for legacy eng-ai-voice references",
      "point operators and docs to voice-builder and voice-truth",
      "preserve historical eval dataset references during migration",
      "raise migration issues when legacy dispatch paths are still used"
    ],
    "cannot_do": [
      "accept scheduled operational work",
      "accept reactive investigation dispatches",
      "propose or certify new voice changes",
      "deploy Retell prompt changes to production",
      "modify voice runtime or locked truth contracts"
    ]
  },
  "execution_scope": "internal",
  "inputs": [
    "legacy worker_id references from runtime or documentation",
    "voice-builder worker spec",
    "voice-truth worker spec",
    "historical eng-ai-voice eval datasets"
  ],
  "outputs": [
    "compatibility redirect notes",
    "migration issues for lingering legacy dispatch paths",
    "historical reference lookups for old eval artifacts"
  ],
  "tools_allowed": [
    "supabase_read",
    "issue_create"
  ],
  "success_metrics": [
    {"name": "legacy_dispatch_count", "target_max": 0, "eval_dataset": "evals/eng-ai-voice/legacy-dispatch-count.json"},
    {"name": "compatibility_redirect_accuracy", "target": 1.0, "eval_dataset": "evals/eng-ai-voice/compatibility-redirect-accuracy.json"}
  ],
  "approval_boundaries": [
    "Cannot run scheduled or reactive operational workflows",
    "Cannot approve, propose, or certify new voice changes",
    "Legacy alias removal requires human review"
  ],
  "autonomy_tiers": {
    "auto_merge": [],
    "agent_review": [],
    "human_review": []
  },
  "scheduled_tasks": [],
  "reactive_triggers": [],
  "context_sources": [
    "knowledge/worker-specs/voice-builder.yaml",
    "knowledge/worker-specs/voice-truth.yaml"
  ],
  "dependencies": [
    "knowledge/worker-specs/voice-builder.yaml",
    "knowledge/worker-specs/voice-truth.yaml"
  ],
  "git_workflow": {
    "branch_pattern": "",
    "commit_prefix": "",
    "pr_labels": [],
    "validated_by": "voice-truth"
  }
}
